/*
  Configure where the backend API lives.
  - Leave as empty string '' to call same-origin (useful when running Flask locally).
  - Set to your deployed backend URL for GitHub Pages, e.g.:
      window.API_BASE = 'https://your-app.onrender.com';
  - Do NOT include a trailing slash.
*/
window.API_BASE = '';